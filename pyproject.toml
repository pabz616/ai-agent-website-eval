[project]
name = "ai-agent-website-eval"
version = "0.1.0"
description = "Monitor website availability with an OpenAI agent"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "openai-agents>=0.22.3",
    "requests>=2.34.2",
]